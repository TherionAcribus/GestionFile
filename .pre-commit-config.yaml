# Scan de secrets avant chaque commit.
# Installation (une seule fois, dans le venv du projet) :
#   pip install pre-commit
#   pre-commit install
# Test manuel sur tout le dépôt :
#   pre-commit run --all-files
# gitleaks lit automatiquement .gitleaks.toml (règles) et .gitleaksignore
# (empreintes des fuites historiques rotées) à la racine du dépôt.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: detect-private-key
      - id: check-added-large-files
        args: ["--maxkb=2000"]
      - id: end-of-file-fixer
      - id: trailing-whitespace
